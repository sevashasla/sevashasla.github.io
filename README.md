# Personal website

A small, dependency-free personal website hosted with GitHub Pages.

## Local preview

Run `python3 -m http.server 8000`, then open `http://localhost:8000`.

The entire site is `index.html`, `styles.css`, and the supporting files in
`images/` and `files/`.
